* Operating System Process Lifecycle
* System Calls (and examples like fork, exec, exit, open, read, write)
* Windowing System Architecture (e.g., X Window System, Wayland, Windows DWM, macOS WindowServer)
* GUI Event Loop or Message Loop
* Windows Messages (specifically for Windows OS, e.g., WM_CREATE, WM_CLOSE, WM_DESTROY, WM_PAINT)
* Signals in Unix (e.g., SIGINT, SIGTERM, SIGHUP)
* Inter-Process Communication (IPC) mechanisms
* Application Frameworks (and how they manage app lifecycle, e.g., Android Activity Lifecycle, iOS UIApplicationDelegate, Cocoa on macOS, Windows API/WinUI on Windows, GTK/Qt on Linux)
* Object-Oriented Operating System Design (though most mainstream OS kernels aren't purely OO, the concepts are influential in higher layers).
* COM (Component Object Model) in Windows: For how applications can expose interfaces and interact.
* D-Bus (Linux)

* working of Linux terminal
* Pseudo Terminal (PTY)
* Standard streams (stdin, stdout, stderr)
* Shell parsing and execution
* ANSI escape codes
* Difference between shell and terminal
* Job control in shells
* Unix process model (fork, exec)
* Process control block
* kernel mode vs user mode
* System call
* Application Binary Interface
* executable and linkable format, portable executable
* 